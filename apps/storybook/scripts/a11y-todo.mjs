console.log("Storybook a11y is configured via @storybook/addon-a11y in todo mode for the first slice.");
console.log("Run Storybook locally and graduate this script to test-storybook once slice violations are zero.");
